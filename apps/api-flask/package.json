{
  "name": "my-flask-app",
  "version": "1.0.0",
  "description": "A Flask app that classifies hotdog and non-hotdog images",
  "scripts": {
    "start": "export FLASK_APP=app.py && python3 -m flask run --host=0.0.0.0",
    "dev": "pnpm start --debug"
  }
}
